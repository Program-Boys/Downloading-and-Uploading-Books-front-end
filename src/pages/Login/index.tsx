import Form from '../../components/Form';
import Header from '../../components/Header';

const PageLogin = () => {
  return (
    <>
      <Header />
      <Form accountSubmit={() => {}} />
    </>
  );
};

export default PageLogin;
